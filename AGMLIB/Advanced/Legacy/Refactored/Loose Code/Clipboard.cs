/*


    void Start()
    {


        foreach (MovementTarget target in targets)
        {
            //StrikeCraft newfighter = Instantiate(fighter).GetComponent<StrikeCraft>();
            //newfighter.target = target;
            //newfighter.manager = this;
            //strikeCrafts.Add(newfighter);   
        }
        foreach(int x in Enumerable.Range(0, 5))
        {
            foreach (int z in Enumerable.Range(0, 5))
            {
                //GameObject target = Instantiate(formationelemnt, transform) as GameObject;

                //target.transform.position += new Vector3((x - 2) * 10, 0, (z - 2) * 10);

                //StrikeCraft newfighter = Instantiate(fighter).GetComponent<StrikeCraft>();
                //newfighter.target = target.GetComponent<MovementTarget>();
                //newfighter.manager = this;
                //newfighter.transform.parent = transform;

            }

        }

    }


*/